before_install:
   - cp ./settings.xml ~/.m2/settings.xml
install:
   - echo "Running a custom install command"
   - (cd yang/yang-data-impl && ../../mvnw clean install -DskipTests)
   - (cd yang/yang-data-codec-gson && ../../mvnw clean install -DskipTests)
